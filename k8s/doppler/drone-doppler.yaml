apiVersion: secrets.doppler.com/v1alpha1
kind: DopplerSecret
metadata:
  name: drone
  namespace: doppler-operator-system
spec:
  tokenSecret:
    name: drone-doppler-token
  managedSecret:
    name: drone-doppler-secret
    namespace: drone
