---
borgmatic_nas:
  name: borgmatic
  container:
    image: ghcr.io/borgmatic-collective/borgmatic:1.8.2
    capabilities:
      - net_admin
    devices:
      - /dev/fuse:/dev/fuse
    volumes:
      - "{{ hostvars[network_svc.borgmatic_nas.host]['common_backup_dir'] | default('') }}/borg:/mnt/repo"
      - "{{ hostvars[network_svc.borgmatic_nas.host]['common_media_dir'] }}:/mnt/media:ro"
      - "{{ hostvars[network_svc.borgmatic_nas.host]['common_dir'] }}:/mnt/app_data:ro"
      - "{{ hostvars[network_svc.borgmatic_nas.host]['common_dir'] }}/borgmatic/etc:/etc/borgmatic.d"
      - "{{ hostvars[network_svc.borgmatic_nas.host]['common_dir'] }}/borgmatic/state:/root/.borgmatic"
      - "{{ hostvars[network_svc.borgmatic_nas.host]['common_dir'] }}/borgmatic/config:/root/.config/borg"
      - "{{ hostvars[network_svc.borgmatic_nas.host]['common_dir'] }}/borgmatic/ssh:/root/.ssh"
      - "{{ hostvars[network_svc.borgmatic_nas.host]['common_dir'] }}/borgmatic/cache:/root/.cache/borg"
    env:
      BORG_PASSPHRASE: "{{ borg_encryption_passphrase }}"
      BORG_RSH: "ssh -i ~/.ssh/borg"
