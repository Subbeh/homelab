---

- name: Create Docker network
  community.docker.docker_network:
    name: "{{ item }}"
    driver: "{{ network_docker.[item].driver }}"
    ipam_config:
      - "{{ networks[item].ipam_config }}"
  loop: "{{ create_network }}"
